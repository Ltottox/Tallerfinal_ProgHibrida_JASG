import { Componentes } from './componentes';

describe('Componentes', () => {
  it('should create an instance', () => {
    const directive = new Componentes();
    expect(directive).toBeTruthy();
  });
});
